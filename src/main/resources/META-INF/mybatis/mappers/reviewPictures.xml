<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.jhta.restaurants_service.vo.review.ReviewPicture">
	
	<insert id="insertReviewPicture" parameterType="ReviewPicture">
	  insert into REVIEW_PICTURES
	  (picture_name, review_id)
	  values
	  (#{pictureName}, #{review.id})
	</insert>
	
	<select id="getReviewPicturesByReviewId" parameterType="int" resultType="ReviewPicture">
		select 
			id,			
			picture_name	as pictureName,	
			review_id		as "review.id"
		from 
			REVIEW_PICTURES
		where 
			review_id = #{value};
	</select>
	

</mapper>