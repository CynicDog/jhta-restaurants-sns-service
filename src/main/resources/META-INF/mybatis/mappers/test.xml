<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.jhta.restaurants_service.mapper.TestMapper">

	<insert id="insertTest" parameterType="kr.co.jhta.restaurants_service.vo.Test">
		insert into TEST
		(id, name)
		values
		(#{id},#{name})
	</insert>

</mapper>